{
  "name": "artifact-server",
  "version": "1.0.0",
  "description": "Self-hosted MCP artifact server — compile JSX/HTML to browsable URLs",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "uuid": "^9.0.0"
  }
}
